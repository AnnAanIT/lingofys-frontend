
import React from 'react';

// This component is obsolete in the pure credit model as we don't split booking revenue categories.
// Replaced with a simple placeholder or empty component if imported elsewhere.

export const RevenueBreakdownTable: React.FC<any> = () => {
    return null; 
};
